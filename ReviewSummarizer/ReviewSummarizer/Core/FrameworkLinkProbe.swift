// Dummy file to verify Foundation Models and SwiftData are linkable.
// Will be deleted in T-013 when the real Core/Logging files are added.

import Foundation
import FoundationModels
import SwiftData

enum FrameworkLinkProbe {
    static let ok = true
}
